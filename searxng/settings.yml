use_default_settings: true

general:
  instance_name: "Viseer local"

search:
  formats:
    - html
    - json

server:
  secret_key: "change-this-secret"
  limiter: false
  image_proxy: false

valkey:
  url: valkey://valkey:6379/0
